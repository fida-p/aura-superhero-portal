# =====================================================
# AURA — Guardian of Voices | Frontend environment
# Copy to `.env.local` (local) — never commit real values.
# =====================================================

# Optional: in a deployed/static build this points the frontend at the
# Express API. Leave unset locally so the Vite dev proxy (/api) is used.
# Example: VITE_API_URL=https://your-api.example.com/api
# VITE_API_URL=