# =====================================================
# AURA — Guardian of Voices | Production build env
# Used by `npm run build` (Vite loads .env.production automatically).
# Set the public URL of the deployed Express API before building.
# NEVER commit real values or credentials.
# =====================================================

# VITE_API_URL=https://your-api.example.com/api